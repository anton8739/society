export  let getGroupsList = (state) => {
    return state.groups.groupsList;
}

export  let getPossibleGroups = (state) => {
    return state.groups.possibleGroups;
}

export let getNumberOfGroups = (state) => {
    return state.groups.numberOfGroups;
}

