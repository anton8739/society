export let getNumberOfAlbums = (state) => {
    return state.photos.numberOfAlbums;
}

export let getAlbums = (state) => {
    return state.photos.albums;
}
export let getNumberOfPhotos = (state) => {
    return state.photos.numberOfPhotos;
}

export let getMyPhotos = (state) => {
    return state.photos.myPhotos;
}